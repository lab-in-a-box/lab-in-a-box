apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wildcard-dns.fullname" . }}-test-connection"
  labels:
    {{- include "wildcard-dns.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wildcard-dns.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
